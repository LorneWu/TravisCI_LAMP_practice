language: c

matrix:
  include:
  - dist: precise
    sudo: required
  - dist: trusty
    sudo: false
  - dist: trusty
    sudo: required
  - dist: xenial
  - os: osx	
  - os: windows
    
script:
  - echo "ok?"
